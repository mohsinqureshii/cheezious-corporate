import { collectionListPage } from '@/components/collections/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Impact metrics' };

export default collectionListPage('impact-metrics');
