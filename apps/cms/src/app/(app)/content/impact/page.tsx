import { collectionListPage } from '@/components/collections/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Impact' };

export default collectionListPage('impact-pillars');
