import { collectionRecordPage } from '@/components/collections/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Press releases' };

export default collectionRecordPage('press-releases');
