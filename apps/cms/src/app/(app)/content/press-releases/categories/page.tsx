import { collectionListPage } from '@/components/collections/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Press release categories' };

export default collectionListPage('press-release-categories');
