import { queueListPage } from '@/components/queues/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Contact submissions' };

export default queueListPage('contact');
