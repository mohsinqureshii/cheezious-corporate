import { queueRecordPage } from '@/components/queues/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Property submissions' };

export default queueRecordPage('properties');
