import { queueListPage } from '@/components/queues/pages';

export const dynamic = 'force-dynamic';

export const metadata = { title: 'Property submissions' };

export default queueListPage('properties');
